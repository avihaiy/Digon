import { Client, Databases } from 'node-appwrite'; import dotenv from 'dotenv'; dotenv.config(); const client = new Client().setEndpoint('https://fra.cloud.appwrite.io/v1').setProject(process.env.VITE_APPWRITE_PROJECT_ID); const databases = new Databases(client); async function test() { try { const res = await databases.listDocuments(process.env.VITE_APPWRITE_DATABASE_ID, 'likes'); console.log('Likes schema:', res.documents[0]); const res2 = await databases.listDocuments(process.env.VITE_APPWRITE_DATABASE_ID, 'comments'); console.log('Comments schema:', res2.documents[0]); } catch(e) { console.error('FAIL:', e.message); } } test();
