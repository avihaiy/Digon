import { Client, Databases } from 'node-appwrite'; const client = new Client().setEndpoint('https://fra.cloud.appwrite.io/v1').setProject('6a6748ef003255232493'); const databases = new Databases(client); async function test() { const DB_ID = '6a674a380025507f9db2'; try { await databases.listDocuments(DB_ID, '6a674a390030977cf6ae'); console.log('profiles ok'); await databases.listDocuments(DB_ID, 'catches_'); console.log('catches ok'); await databases.listDocuments(DB_ID, 'locations'); console.log('locations ok'); await databases.listDocuments(DB_ID, 'tournaments'); console.log('tournaments ok'); await databases.listDocuments(DB_ID, 'store_items'); console.log('store items ok'); } catch(e) { console.error('FAIL:', e.message); } } test();
