import { Client, Databases, Query } from 'node-appwrite'; const client = new Client().setEndpoint('https://fra.cloud.appwrite.io/v1').setProject('6a6748ef003255232493'); const databases = new Databases(client); async function test() { const DB_ID = '6a674a380025507f9db2'; try { await databases.listDocuments(DB_ID, 'catches_', [Query.greaterThanEqual('$createdAt', new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString()), Query.limit(500)]); console.log('catches filter ok'); } catch(e) { console.error('FAIL:', e.message); } } test();
