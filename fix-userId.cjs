const fs = require('fs'); let content = fs.readFileSync('src/pages/fishing/Profile.tsx', 'utf-8'); content = content.replace('const { userId } = useParams();', 'const { userId: urlUserId } = useParams();\n  const { user: currentUser, updateProfileField } = useAuth();\n  const userId = urlUserId || currentUser?.;\n').replace('const { user: currentUser, updateProfileField } = useAuth();', '').replace('const isOwnProfile = currentUser?. === userId;', 'const isOwnProfile = currentUser && currentUser. === userId;'); fs.writeFileSync('src/pages/fishing/Profile.tsx', content);
